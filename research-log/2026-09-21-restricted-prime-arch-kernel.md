# 2026-09-21 — Restricted Prime–Archimedean Kernel Target

The pole-neutral reduction removes two previously conflated components:

1. the pole quadratic is exactly zero on the pole-neutral row;
2. the prime-power singular rank-one curvature is exactly zero because its quadratic action is \(M_0(v)^2\).

The remaining exact finite form is therefore
\[
Q_{\mathrm{PN}}(v;c)
=
-\sum_{q=p^a\le c}\frac{\Lambda(q)}{\sqrt q}
K_v\!\left(1-\frac{\log q}{\log c}\right)
+
\frac1{2\pi}\int_{\mathbb R}h_+(r)g_v(r)\,dr.
\]

This is now the principal analytic sign target for the even pole-neutral route.

No sign is assumed for either term separately, and no RH conclusion is drawn.
