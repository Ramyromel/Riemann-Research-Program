# Research Log — Prime-Intersection Autocorrelation Bridge

**Date:** 2026-09-22

## Action

The \(2x-1\) route was advanced from generic kernel testing to the exact Prime–Weil object already present in the repository.

## Result

The exact finite dictionary uses
\[
\omega_q=1-\frac{\log q}{\log c},
\qquad
K_v(\omega)=2\int_0^\omega T_v(t)T_v(\omega-t)\,dt.
\]

The quantity \(K_v\) is an overlap/correlation between \(T_v\) and its reflected copy on the common interval. The prime side samples this object at the prime-power locations \(\omega_q\) with weights \(\Lambda(q)/\sqrt q\).

This gives a mathematically grounded interpretation of the user's intersection idea.

## Boundary

No equality was asserted between \(K_v(\omega_q)\) and an arbitrary pairwise source Gram kernel. Such an equality would require an explicit support/extension construction and must be derived.

## Consequence

The research target is narrowed to an exact positive Gram/overlap representation of the pole-neutral Prime–Weil form, with the archimedean term supplying the completion.

No zeta zeros were used. No RH claim is made.
