# Hilbert–Pólya / Weil Normalization Audit

**Status:** ACTIVE AUDIT — NO SPECTRAL CLAIM

## Executive finding

The newly identified finite Hilbert–Pólya construction of Yaoming Shi (arXiv:2609.04908, 2026) is structurally aligned with this repository: finite real-symmetric Prime–Weil matrices, pole/archimedean/prime-power contributions, and a Hermitian definite generalized eigenproblem on a fixed zero-mean contrast space. The external abstract also states that the zero-side interpolation experiment is a reconstruction theorem and that the remaining transfer problem requires a relative prime-to-zero perturbation theorem with uniform control of the compressed metric. citeturn0academia0

This audit therefore does **not** treat the external finite matrix as an already-proved Hamiltonian.

## 1. Normalization objects already fixed in this repository

The project finite Weil normalization already fixes:

- arithmetic cutoff (c>1);
- (L=log c);
- Galerkin dimension (N);
- real-even coefficient vector (v);
- exact source-to-test-function map (vmapsto g_v);
- pole-neutral condition (M_0(v)=0);
- exact restricted prime functional
[
Q_{mathrm{prime}}(v;c)
=
-sum_{q=p^ale c}
rac{Lambda(q)}{sqrt q}K_v(omega_q);
]
- exact archimedean functional
[
Q_{mathrm{arch},infty}(v;c)
=
rac1{2pi}int_{mathbb R}h_+(r)g_v(r),dr.
]

The external finite dictionary states that every real-even Galerkin vector determines a band-limited Guinand–Weil test function whose zero-side sum equals the corresponding finite quadratic value exactly. citeturn0academia1

## 2. What is verified conceptually

### V1 — Common arithmetic source

Both approaches use the Weil explicit formula and retain prime-power information rather than fitting the known zero ordinates.

### V2 — Quotient philosophy

The external construction uses a fixed zero-mean contrast space. Our finite Weil route independently contains an exact pole-neutral subfamily defined by (M_0(v)=0).

This is a **candidate correspondence**, not yet a theorem of equality between the two quotient constructions.

### V3 — Spectral object type

The correct finite spectral object is a generalized eigenproblem
[
Sx=lambda Gx
]
with a positive-definite metric (G), rather than an arbitrary declaration (H=Q).

## 3. What remains unverified

### U1 — Entry-by-entry matrix identity

We have not yet established
[
S^{mathrm{Shi}}_{N,c}
=
mathcal S(Q_infty;N,c)
]
entry by entry under the exact repository basis and sign convention.

### U2 — Metric identity

We have not established that the external compressed metric (G_N) is identical to the metric naturally induced by the repository's source quotient.

### U3 — Constraint equivalence

We have not proved that the external zero-mean contrast constraint and the repository condition
[
M_0(v)=v_0+sqrt2sum_{k=1}^Nv_k=0
]
are the same constraint after the respective basis maps.

### U4 — Trace identity

No exact finite identity has yet been derived showing that
[
operatorname{Tr}f(H_{N,c})
]
reproduces the repository's prime/pole/archimedean decomposition for a declared test-function class.

### U5 — Infinite limit

No operator convergence or spectral-measure convergence theorem has been established.

## 4. Critical anti-circularity rule

Known zero ordinates (gamma_k) may be used only in a separate reconstruction-control experiment.

They must not be used to:

- tune arithmetic matrix entries;
- choose (c,N);
- fit free parameters;
- select a basis after inspecting the target spectrum;
- claim discovery of a Hamiltonian.

The external paper itself distinguishes the zero-input interpolation result from an arithmetic proof. citeturn2search0

## 5. New load-bearing theorem target

The immediate mathematical target is now:

> **Finite Prime–Weil equivalence theorem:** under one explicitly stated normalization and basis map, derive the finite arithmetic pencil ((S_{N,c},G_N)) directly from the project's exact Weil quadratic form and prove equivalence to the externally described Prime–Weil quotient construction.

This is a finite theorem. It does not require RH.

If it fails, the discrepancy must be classified as one of:

- normalization mismatch;
- basis mismatch;
- quotient mismatch;
- sign mismatch;
- metric mismatch;
- genuinely different finite constructions.

## 6. Why this target is preferable to immediate zero fitting

A finite spectral match can be manufactured from supplied zeros. Such a match therefore has weak evidentiary value by itself.

An exact arithmetic equivalence is stronger: it establishes that the candidate spectral object is actually generated by the same explicit-formula data that control the Weil route.

The external literature independently identifies the relative prime-to-zero perturbation and compressed-metric control as the remaining central transfer problem. citeturn3search9

## 7. Status ledger

| Item | Status |
|---|---|
| Finite Hilbert–Pólya architecture | DERIVED DESIGN |
| External Prime–Weil construction exists | VERIFIED |
| Repository finite Weil normalization | DERIVED / AUDITED |
| External/project matrix identity | OPEN |
| External/project quotient identity | OPEN |
| Positive metric equivalence | OPEN |
| Arithmetic trace identity | OPEN |
| Infinite-dimensional self-adjoint operator | OPEN |
| Spectral identification with zeta zeros | OPEN |
| RH | OPEN |

**No proof claim.**
